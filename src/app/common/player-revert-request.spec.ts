import { PlayerRevertRequest } from './player-revert-request';

describe('PlayerRevertRequest', () => {
  it('should create an instance', () => {
    expect(new PlayerRevertRequest()).toBeTruthy();
  });
});
